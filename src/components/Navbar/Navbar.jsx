import React from "react";
import "./Navbar.css";

// We don't need to creat a nav bar since it is already created

const Navbar = () => {
  return <div>Navbar</div>;
};

export default Navbar;
